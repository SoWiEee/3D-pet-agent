"""Optional research backends (spec §14).

Heavy / optional — never imported by the mainline perception path unless
explicitly enabled (e.g. ``PET_AGENT_POSE_SOURCE=slam`` for the Visual SLAM
sidecar in §14.1).
"""
